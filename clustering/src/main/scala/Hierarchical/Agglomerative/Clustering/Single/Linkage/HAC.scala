package Hierarchical.Agglomerative.Clustering.Single.Linkage

/*
  Use Euclidean Distance, and draw the dendrogram.

  1. Calculate the Euclidian Distance, create the Distance Matrix
      d(x, y) = rad( (x.x - y.y)^2 + (x.y - y.y) ^ 2 )
 */

object HAC {

}
